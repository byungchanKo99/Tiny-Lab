name: example
description: "Example project — replace with your own experiment"

build:
  type: flag

run:
  type: command

evaluate:
  type: stdout_json

baseline:
  command: 'echo ''{"metric": 1.0}'''

metric:
  name: metric
  direction: minimize

levers:
  example_lever:
    flag: "--example"
    baseline: 1.0
    space: [0.5, 1.0, 1.5, 2.0]

rules:
  - "Install required dependencies before starting the loop"
